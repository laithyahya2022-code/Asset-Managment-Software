# Copy to .env and adjust
SECRET_KEY=generate-a-long-random-string
# Leave DATABASE_URL unset to use a local SQLite file in instance/
# DATABASE_URL=postgresql://itam:password@localhost:5432/itam
# UPLOAD_FOLDER=/data/uploads
# BACKUP_FOLDER=/data/backups
